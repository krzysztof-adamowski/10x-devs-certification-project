<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">TenExCards</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="circuit-check">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Circuit check
            </NavLink>
        </div>

        @* /db-check's ONLY entry point, deliberately. /circuit-check is reachable from here AND
           from an in-body link on Home.razor, so S-01's deletion list is longer than it looks.
           Do not add a second link to this one. *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="db-check">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Database check
            </NavLink>
        </div>
    </nav>
</div>

